Install via npm:
  npm i releasify -g

Usage: releasify <command>

where <command> is one of:
  draft, publish, config

releasify <command> -h 
  quick help on <command>

Usage via npx:
  npx releasify <command> args